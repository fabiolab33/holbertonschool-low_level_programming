This project focuses on learning dynamic memory allocation in C using malloc and free.

Files:

0-create_array.c
Funtion that creates an array of chars and initializes it with a spec.

1-strdup.c
Funtion that returns a pointer a newly allocated space in memory.

2-str_concat.c
Funtion that concatenates two strings and stores them in a newly allo.

3-alloc_grid.c
Funtion thats return a pointer to a two dimensional array of integers.

4-free_grid.c
Funtion that frees a two dimensional grid previously created by 'alloc_