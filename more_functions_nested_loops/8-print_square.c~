#include "main.h"

/**
 * print_square - print a square using #
 * @size: Size of the square
 *
 * Description: If the size is 0 or less, print only a new line
 */
void print_square(int size)
{
int i, j;

if (size <= 0)
{
_putchar('\n');
return;
}

for (i = 0; i < size; i++)
{
for (j = 0; i < size; j++)
{
_putchar('#');
}
_putchar('\n');
}
}
